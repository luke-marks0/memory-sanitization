# Python Port Staging Area

This directory is reserved for deterministic Python Filecoin components that
have clear reference boundaries and parity evidence.

Rules:

- production mode remains tied to the vendored Rust reference path;
- deterministic mirrors may graduate here once parity vectors exist;
- no component is promoted without explicit parity review.

